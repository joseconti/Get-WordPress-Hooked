Solo hay que colocar este código en el archivo functions.php de un theme de WordPress y utilizar el shortcode [imprime_hooks] o [imprime_filters] en una página o entrada.

Esto hará que se muestren todas las funciones enganchadas a los hooks propios de WordPress o las que utilicen filters.